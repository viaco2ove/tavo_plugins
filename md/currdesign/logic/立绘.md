轮到角色说话时那行代码查找立绘和显示立绘

  Thought for 1m 4s, searched for 2 patterns, read 2 files

轮到角色说话时查找和显示立绘的核心代码

在 plugins\toonflow_story_sprite_background\entry.js 中：

1. 触发点

第315行 - 监听消息添加事件：
tavo.plugin.on('message:added', async (event) => {

2. 查找立绘

第287-313行 resolveSpeaker() - 确定发言者：
function resolveSpeaker(msg) {
  // 1. 优先读 tf_last_speaker（MCS插件写入）
  const lastSpeaker = readVar('tf_last_speaker');
  if (lastSpeaker && lastSpeaker.name) return { name: lastSpeaker.name, id: lastSpeaker.characterId };
  // 2. 从消息的 characterName
  if (msg && msg.characterName) return { name: msg.characterName, id: msg.characterId };
  // 3. 有 characterId 则反查 name
  ...
}

第236-248行 - 从 tf_sprites 变量查角色立绘：
const sprites = readVar(NS) || {};  // NS = 'tf_sprites'
const byName = sprites.byName || {};
const entry = byName[speakerName] || null;  // 用角色名找立绘条目

tf_sprites 格式：
{ byName: { "红缥缈": { fg: "files/xxx.png", bg: "files/bg.webp", id: "char_123" } } }

3. 显示立绘

第217-279行 updateSprite() - 主渲染函数：
function updateSprite(speakerName) {
  // ...
  let fgPath = entry && entry.fg ? entry.fg : '';  // 取前景立绘路径
  setForeground(fgPath);  // 显示到 DOM
  setBackground(bgPath);  // 设置背景
}

第184-214行 setForeground() - 操作 DOM 显示：
function setForeground(fgPath) {
  const fgImg = document.getElementById('tf-sprite-fg');
  if (fgPath) {
    const url = resolveUrl(fgPath);  // 解析成完整URL
    fgImg.src = url;                 // 设置图片src
    fgImg.classList.remove('hidden');// 显示
  } else {
    fgImg.classList.add('hidden');   // 隐藏
  }
}

数据流总结

新消息 → message:added事件 → resolveSpeaker()获取角色名
                              ↓
                        tf_sprites.byName[角色名]
                              ↓
                        取 entry.fg 路径
                              ↓
                        setForeground(fgPath)
                              ↓
                        document.getElementById('tf-sprite-fg').src = url