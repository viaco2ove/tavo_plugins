我发现缺少了旁白这个角色。增加进去。toonflow game 是系统自带。 tavo 貌似没有？
还是要创建一个toonflow game 一样功能的旁白角色。


`role_type`：角色类型，如 `npc` / `narrator` / `player` / `system` /`general`
 也就是 `一般角色`/`旁白`/`用户`/`系统角色`/`万能角色`

数据最初来源：角色卡
插件生成的更可读json:
tmm_story_static_{chatId} scope:global