# 文件的mcp 上传,tavo 上传，tavo 获取，使用，绑定的变量
# 图片读取方式：
- 可用 files/xxxx
 <img id="tf-sprite-fg" class="tf-sprite-fg" src="files/chat/sprite_fg_51.png" alt="sprite" style="bottom: 0px; height: 100vh;">
- 也可以说base64

# tavo 界面上的文件上传
文件上传 — 改用 tavo.file.import（系统文件选择器，比 <input type=file> 可靠）

# mcp 上传头像 （tavo ui 类似，但用的tavo.file.import 再用 tavojs 绑定变量）
先要创建角色或用户身份
然后上传头像图片
再把这个头像图片绑定到角色或用户身份上

# 当前章节图片
根据 tf_progress的currentChapterIndex 获取图片
# 章节的背景图片
mcp上传图片/tavo ui 上传图片
设置 tf_story.edit{"chapters"[{"background":"xxx"}]} 例如：files/chat/chapter_bg_1.jpg


## 用户身份的头像和立绘和音色文件
 {persona: {...}} 格式 avatar 


# 其他文件的变量和获取方式

