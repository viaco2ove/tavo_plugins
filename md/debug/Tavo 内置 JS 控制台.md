# Tavo 内置 JS 控制台
步骤：打开左侧侧边栏-更多-聊天设置-javascript 控制台 改为开启
就是日志查看器。你在插件里埋 console.log('走到这一步了')、
console.error(报错信息)，在 Tavo 的设置里打开控制台看输出。
用途只有一个：确认插件在真实环境里到底跑没跑、跑到哪、报什么错。
仅此而已——不能看 DOM(实际上也不是完全不可以)、不能改样式、不能断点。