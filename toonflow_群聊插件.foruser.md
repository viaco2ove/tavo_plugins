# toonflow 群聊插件

让 **Tavo** 群聊跑出「可推进剧情的 AI 角色扮演」。  
插件读完你的剧情大纲，自动安排角色发言、推进剧情、记住重要信息，还能合成语音。

---

## 它能做什么

| 场景 | 效果 |
|---|---|
| 写了一部玄幻小说大纲 | 进群聊后，AI 按大纲推进剧情，旁白、男女主、配角轮流登场 |
| 不想自己演 NPC | AI 自动安排谁说话、说什么、什么时候停下等你 |
| 玩到某一章结束 | 自动切下一章；全部章节结束后进入「自由对话」模式 |
| 角色状态变化（升级/受伤/换装备） | 自动记到记忆里，下一轮继续生效 |
| 想听配音 | 自动按句切分合成 TTS，逐句播放 |

---

## 一句话理解架构
用mcp 安装 或者自己去tavo app的插件库下载 Toonflow-群聊-xxx 插件。
```
你的剧情大纲  →  编排师（决定谁说）  →  发言器（写台词）  →  群聊消息
                                                          ↓
                                          记忆管理器（记住）
```

---

## 三步开始使用

### 1. 准备好你的剧情[让ai 写就行]

新建一个文件夹，比如 `我的小说/`，里面放两个东西：

**① `story_sync_config.json`** — 告诉插件基本信息

```json
{
  "story_name": "我的小说",
  "chat_name": "我的小说",
  "response_mode": "scenario",
  "persona": { "name": "你的名字", "personality": "玩家" },
  "characters": [],
  "worldbook": { "name": "世界设定", "intro": "" },
  "chapters": { "dir": "chapters", "enabled_first_only": true }
}
```

**② `chapters/01_第一章.md`** — 剧情正文

```markdown
## 开局
### 醒来
@旁白：你睁开眼，发现自己躺在破庙里……

### 用户发言
（请决定下一步行动）

## 第一场战斗
### 敌人出现
@旁白：远处传来脚步声……

### 用户发言
（继续）
```

**格式就两条**：
- `## 标题` = 剧情阶段
- `### 标题` = 小事件  
- 含「用户发言」的小节 = 轮到玩家做决定
- `@角色名：台词` = 该角色的对白或旁白

### 2. 同步到 Tavo

```bash
cd {项目根目录}
# 让ai 帮你安装需要的环境工具
# 然后
python -m tavo_plugins sync --story-json "story.json" --force --skip-plugins
```

CLI 自动完成：建群聊 → 同步角色 → 同步世界书 → 导入章节 → 初始化记忆。

### 3. 开聊

直接打开 Tavo 群聊，插件已就位。输入对话，剧情就自己动起来了。

---

## 玩法小技巧

| 输入 | 含义 |
|---|---|
| 正常对话 | 推进剧情 |
| `.` | 跳过本轮（我不想互动，直接推） |
| `@角色名 xxx` | 强制让该角色说话 |
| `@记忆管理 xxx` | 手动调整记忆（如「更新主角等级」） |

---

## 我能看到什么

进群聊后会多出两个浮动按钮（可拖动定位）：

- **📖 剧情面板** — 显示当前进度（哪一章、哪一阶段、下一步）
- **💬 气泡样式** — 切换「单条/多条显示」+ 调透明度

底部会出现 `角色发言`、`记忆管理` 等日志标签，所有 AI 调用过程透明可见。

---

## 常见问题

**Q：能不写大纲就玩吗？**  
A：可以。空章节进去就是「自由模式」，AI 自由对话，没有剧情约束。

**Q：想让某个 NPC 早点登场？**  
A：在章节大纲里把它放进对应阶段即可，编排师会按权重挑角色发言。

**Q：角色状态错乱了怎么办？**  
A：发 `@记忆管理 修正 xxx 的状态为 yyy`，记忆管理器会直接改写并保存。

**Q：怎么暂停语音？**  
A：插件设置里关掉「自动播放」即可，文字输出不受影响。

---

## 项目地址

https://github.com/viaco2ove/tavo_plugins.git