# 2026-08-14 工作日志

## 任务：将参考故事推送到 tavo（世界书=故事蓝图 + 多角色）

把 `Toonflow-game-story/ai_story/171/谁让这个山大王修仙的` 暂存进 `.cache/story/谁让这个山大王修仙的`，并用 tavo MCP 创建角色/世界书/群聊，达成 `md/currdesign/toonflow_story_multi-character_stage/design.md` 效果。

### 关键过程与结论
- tavo MCP 地址：`.env` 初为 `10.153.53.105:7347`（不可达），后改为 `10.10.2.208:7347`。本机可 ping 通 `10.10.2.208` 但 TCP 7347 一度未监听（tavo 未开 MCP Server）；用户开启后连通（HTTP 200，95 个工具）。
- **推送成功**（2026-08-14 17:4x）：
  - 12 角色 id=7~18（纯小白 player + 红缥缈/白锦儿/李玄风/陆青山/云火月/林月/琳琅/冷素心/苍山道人 9 NPC + 某女子/某男子 2 general）
  - 世界书 id=1，45 entry（11 constant + 34 keyword）
  - 群聊 id=6，绑定 12 角色 + 世界书，responseMode=scenario
- 验证：`tavo_lorebook_get(1)` 返回 45 entries；`tavo_chat_search` 返回群聊 characterIds=[7..18]、lorebookIds=[1]。

### 踩坑 & 字段约定（重要，复用避免再踩）
- tavo create 用 CCv3 形状；chat 字段是 **camelCase**：`characterIds` / `lorebookIds` / `responseMode`（非下划线）。
- lorebook entry 用 **Tavo-native 形状**：必填 `identifier` + `content` + `strategy`(enum `constant`/`keyword`)；关键词字段是 `keywords`（复数）；`completion_condition` 非标准字段会被忽略。
- create 返回 id 在 `result.content[0].text` 的 JSON 字符串里的 `"id"` 字段（非 character_id）。
- 去重用 `tavo_character_search` / `tavo_lorebook_search`（返回 `{items:[{id,...}], nextCursor}`）。
- 注意 `.env` 键名是 `tavo_mcp_toekn`（toekn 拼写）。

### 产物（.cache/story/谁让这个山大王修仙的/）
- `tavo_story_payload.json`（12 角色 + 45 entry 世界书载荷）
- `build_tavo_payload.py`（从 worldbook/chapters/roles 重新生成载荷）
- `push_to_tavo.py`（推送脚本，支持 --check/--dry/正式，带角色+世界书去重可安全重跑）
- `push_result.json`（chat_id=6, lorebook_id=1, character_ids 7-18）

## 任务：整理 tavo MCP 工具文档
- 拉取 tavo MCP `tools/list`（共 95 个工具），按 15 个分类整理。
- 产物：`md/tavo_help/mcp_help/mcp_help.md`（分类速览+详细清单）与 `mcp_help.csv`（工具名/分类/中文用途/英文说明，utf-8-sig）。
- 生成脚本临时放在 `.cache/story/_gen_mcp_help.py`，原始列表 `.cache/story/_tavo_tools_list.json`。
- 分类：系统状态(3) 文件存储(5) 角色(6) 世界书(8) 正则脚本(9) 生成预设(9) 用户人格(6) 对话/群聊(9) 对话主题(7) 消息(6) 变量(5) 长期记忆(3) 输入框(5) 生成与语音(4) 插件(10)。

## 任务：tavo 端污染溯源与修复
- 用户截图显示群聊 6 里：顶部 toast「进入章节『青云决（功法）』」+ 大量署名"纯小白"的「场景切换至 XXX」消息 + 「故事进度」浮窗（未绑定故事+重新开始）+ 弹框「是否允许添加加聊天消息？」+ 角色头像全空。
- **元凶 = `toonflow_story_event_manager` 插件**（pluginId=`com.toonflow.story-event-manager`，name=`Toonflow-事件管理器`，version=1.0.0）。`plugins/toonflow_story_event_manager/entry.js`:
  - `initStory()` 把所有非 `strategy=constant` 的 worldbook entry 都当章节；我们 34 条 keyword entry 全部被它识别为"34 章"
  - `advanceChapter()` line 260-263: `tavo.message.append({content:'（场景切换至 ${name}）', hidden:false})` → 自动插入消息
  - line 269: `tavo.utils.toast(tavo.plugin.i18n.t('runtime.chapterAdvanced',...))` → 顶部 toast
  - manifest 注册 `htmlFragments: tf-story-panel → /chat/body/end` → "故事进度"浮窗
  - 弹框「是否允许添加加聊天消息？」是 tavo 原生权限确认（插件每次调 `message.append` 都弹），"加加"是 tavo i18n 错字
- **角色头像空**与插件无关：我之前推送 12 角色时 Tavo-native `character` 形状只传了 `{name, description, first_mes, personality, roleType}`，**漏了 `avatar`**。修法：`tavo_character_update(id, character:{name, avatar:uri})`。
- **修复**（MCP 通后跑）：
  - `tavo_plugin_set_enabled(pluginId, enabled=false)` 关闭插件
  - `tavo_message_find(chatId, filter:{query:"场景切换至"})` 精准查 → `tavo_message_delete(chatId, id)`
  - 实际执行：插件 enabled/autoAdvance/showPanel 全部置 false；群聊 6 删了 27 条垃圾消息
- 产物：
  - `.cache/story/谁让这个山大王修仙的/cleanup_tavo.py`（关闭插件 + 删垃圾消息 + 可选 --avatar-dir 补头像）
  - `md/tavo_help/plugin_help/toonflow_story_event_manager_副作用与关闭指南.md`（症状/根因/修复方案）
- **MCP 字段名约定补充**（重要，复用避免再踩）：
  - `tavo_plugin_get/set_config/set_enabled` 入参是 `pluginId`（非 `id`）
  - `tavo_message_delete` 入参是 `chatId + id`（非 messageId）；`tavo_message_find` 用 `chatId + filter.query` 子串搜索
  - `tavo_character_update` 入参是 `id + character`（非 characterId）
- **其他装的插件**：`com.toonflow.story-memory-manager`（也有 `tmm-panel` 浮窗在 /chat/body/end，可能也污染）、`com.toonflow.multi-character-stage`（即 design.md 那个，**id 是 `com.toonflow.multi-character-stage`，不是 `toonflow_story_multi_character_stage`**）、`com.toonflow.story-speaker`（群聊角色发言）。

## 任务：确立 tavo_plugins 设计「靠山」映射（跨项目参考约定）
用户明确指定：做 tavo_plugins 时 UI/业务/提示词分别对表三个真源，不要另起炉灶。
- UI → `Toonflow-game-web`（web_project_windows）：`D:\Users\viaco\tools\Toonflow-game\Toonflow-game-web`（src/api|components|composables 为前端界面真源）
- 业务 → `toonflow-game-app`（current_project_windows）：`D:\Users\viaco\tools\Toonflow-game\toonflow-game-app`（src/lib 含 gameEngine.ts/fixDB.ts/fixDB.prompts.ts/roleParameterCard.ts 等引擎真源）
- 提示词 → 两份文件：① `toonflow-game-app\md\curr_design\剧情编排\自由模式\世界书\复刻酒馆\世界知识_agent.md`（世界知识注入设计）；② `toonflow-game-app\src\lib\fixDB.prompts.ts`（多 Agent 剧情编排提示词库）
- **关键约束**：世界知识=上下文注入不是脚本章节（`content` 才发给模型，`agentList` 控注入范围，constant 全收/非 constant 按 keys 匹配 scanText；tavo lorebook 的 `keywords` 对应 Toonflow 的 `keys`）；多 Agent 编排守 NPC优先/万能角色不可替具体角色/数值纯数字。
- 产物：`md/currdesign/design_reference_map.md`（索引+约束），并写入 `MEMORY.md` 长期约定。

## 任务：头像上传排查（18:0x，未完成）
- 用户问"头像都上传好吗" → 答：**没有**。实查 12 角色 id 7–18 的 `data.avatar` 全部 `null`。
- 素材齐全：参考故事 `avatars/` 12 张 PNG 已复制到 `.cache/story/谁让这个山大王修仙的/avatars/`，与角色一一对应。大小 111KB~1.22MB（白锦儿/红缥缈/纯小白 ≈1.1MB 偏大）。
- **关键发现（已实测）**：`tavo_character_update(id, character:{...avatar...})` 写不进 `avatar` —— 塞 1×1 极小 data URI 也返回 ok 但 get 仍 null；`files/global/xxx.png` 路径引用也不行。但此结论需复核（见下 MCP Server.md 条款）。
- 网络：tavo MCP 间歇性——传 1.1MB 大图 dataUrl 后 `ConnectTimeout`，需用户保持 tavo 桌面端 MCP Server 开。
- 产物：`avatar_status.md`（现状+排查+方案）、`upload_avatars.py`（待联网跑）。

## 读 MCP Server.md 后修正认知（重要）
- 源：`md/tavo_help/plugin_help/MCP Server.md`（docs.tavoai.dev/cn/guides/mcp-server/）。
- **MCP Server 默认关闭** → 彻底解释了连接超时/断开：tavo 桌面端 MCP Server 需手动开启（设置→MCP Server→选访问范围→启用）。**重新启用时 URL/IP 可能变**（DHCP），故每次联网前必须重读 `.env` 的 `tavo_mcp_url`，不要缓存旧 IP。
- **角色写入工具支持 裸 CC 兼容 data / 完整 CCv2/CCv3 wrapper / SillyTavern 角色 wrapper**（line 21）。即 `avatar` 是头像一等字段，路径就是 CCv3 的 `data.avatar`（data URI）。**这反驳了"update 不支持头像"的猜想**——之前失败更可能是：①传了完整 `data` 对象带计算字段导致 update 忽略 avatar；②1.1MB 超限被静默丢弃（1×1 测试那次 MCP 是否真连上存疑）。
- 文件工具（tavo_file_save 等）**必须传 chatId，即使 scope=global**（之前我传 chatId=6 正确）。
- 导入冲突默认报错，可 `conflict:"overwrite"`/`"saveAs"` 重试。
- **修正后的头像方案**：联网后用 `upload_avatars.py` —— 先把 >300KB 压缩到 ~280KB，再拉 `tavo://schemas/character` 确认真实字段；优先 `tavo_character_update` 写 `data.avatar`，验证仍空则回退 `tavo_character_import`（CCv3 wrapper + conflict:overwrite）。

## 头像上传成功（18:2x，已完成）
- **关键机制（实测，重��复用）**：
  - `tavo_character_update` 任何形式（裸 data / 完整 CCv3 wrapper）都**不持久化 `avatar`**（只同步文本字段）。
  - `tavo_character_import_card` 是唯一能存头像的路径，且为**整卡替换**：须传「完整 `data` 对象 + avatar」，只传 `{name,avatar}` 会丢性格/对白。
  - 它按 name **新建而非覆盖** → 产生重复卡。故流程 = 导入新卡 → `tavo_chat_update(id, chat:{characterIds:[新ids]})` 重绑 → `tavo_character_delete` 删旧卡。
  - import 工具真名是 `tavo_character_import_card`（非 `tavo_character_import`）；其入参无 `conflict` 字段（conflict 仅 theme）。
  - 头像 data URI：原图 1.1MB 太大，压到 87–201KB（PNG 无损优化不够则转 JPEG q≈80–88）。
- **执行结果**：12 角色 id 7–18 → **20–31**，头像全部写入、字段保留（校验 12/12）；群聊 6 `characterIds=[20..31]`、`lorebookIds=[1]`；旧卡 7–18 及探测误建 19 已删。PIL 11.3.0 可用。
- 产物：`upload_avatars_v2.py`（导入+重绑+清理，支持 --dry）、`push_result.json` 已更新为 20–31。

## 头像仍空：根因推翻（18:2x 之后，进行中）
- 用户反馈头像仍空。重连实测：`tavo_character_get` 回看 id 20–31 的 `data.avatar` **确有 data URI**（119–275KB），7–18 已删，群聊 6 绑 [20..31] 正确。
- **根因不是"没上传"，而是"格式不被渲染"**：对比 3 个内置正常角色（id 3/4/5），其 `data.avatar` 是 **`charaCard/1781412865023.png` 文件引用**（27 字符），而非内嵌 base64。推论 Tavo 客户端只渲染 `charaCard/...png` 资源引用，不渲染 data URI。
- **待连上 MCP 验证的正确做法**：最终 `data.avatar` 必须是 `charaCard/...png` 文件引用。候选机制：①用「完整 CCv3 wrapper」导入触发服务端把 dataUrl 自动提取成 charaCard 资源并重写 avatar 字段（裸 data 导入不会触发，故之前失败）；②`tavo_file_save` 落文件 + 引用返回路径（命名空间待确认，未必是 charaCard）。
- 已更新 `md/tavo_help/mcp_help/mcp_头像.md`（加 ⚠️ 重大修正段 + 修正"data URI 直接可用"旧说法 + 用户补的结论第 10 行）。
- 诊断脚本：`_diag_avatar.py`/`_diag_avatar2.py`/`_diag_schema.py`/`_diag_exp.py`（均位于 `.cache/story/谁让这个山大王修仙的/`，纯 stdlib/urllib）。
- **当前阻塞**：18:2x 末 MCP 连超时（WinError 10060，Server 被关或 IP 变）。需用户重开 Tavo 桌面端 MCP Server（设置→MCP Server→启用），并确认 `.env` 的 `tavo_mcp_url` 是否仍 `http://10.10.2.208:7347/mcp`（IP 可能变，变了请告知）。重连后跑 `_diag_exp.py` 确认机制再批量重传。

## 头像最终修复完成（18:3x，已完成）
- 用户"直接重试"后 MCP 重连成功。实验确认：
  - **`charaCard/` 命名空间 MCP 碰不到**（那只是 Tavo 原生导入器用的，内置角色才用）。
  - **真正生效格式 = `files/global/<name>`**：`tavo_file_save({chatId, name, content(base64), options:{scope:'global', encoding:'base64'}})` 落文件并返 `files/global/<name>`；用它当 `data.avatar` 导入，客户端**能渲染**。
  - 完整 CCv3 wrapper 导入**不会**自动把 base64 提取成资源（服务端原样存 dataUrl）→ 这条路死。
- 对照测试：纯小白 id20 → 新卡 **34**（`files/global/纯小白.jpg`），用户确认界面显示 → 机制成立。
- 批量重传 `upload_avatars_v3.py`：从库里旧卡(21–31)的 data.avatar 解出 base64 → `tavo_file_save` 落 `files/global` → `import_card` 重导 → 重绑群聊 → 删旧卡。结果：
  - 红缥缈35/白锦儿36/李玄风37/陆青山38/云火月39/林月40/琳琅41/冷素心42/苍山道人43/某女子44/某男子45；纯小白34；共 12 张，校验「渲染可用=YES」11/11（34 已先验）。
  - 群聊 6 `characterIds=[35..45, 34]`、`lorebookIds=[1]`；旧卡 21–31 已删（7–18/19 之前已删）。
- 验收标准升级：**以界面真显示为准**，不再只看库里有无值。`mcp_头像.md` 已全文订正（含废弃 v2 提示）。
- 用户反馈：`mcp_头像.md` 多余信息太多，要求**只保留「创建时上传 / 更新时上传头像」两个场景**，已大幅精简（去掉踩坑史、执行结果、环境约定等）。后续文档也应同理——只写怎么做，不写来龙去脉。

## 四个插件按设计图对齐（已完成）
- 用户选「四个按设计图对齐」。读两份真源（世界知识_agent.md + fixDB.prompts.ts）后改四个插件源码并重建 .tpg。
- **统一记忆契约(tmm)**：memory_manager 存 `tmm={summary,facts,tags,cards:{player,npcs},...}`；multi_character_stage/speaker 改为读 `tmm.summary`/`tmm.cards.npcs[name]`（修掉原 `tmm.meta.*` 错误路径，原跨插件读取全失效）。
- **memory_manager** 对齐 story_memory 人设：长期记忆+角色参数卡、数值纯数字、输出 JSON{summary,facts,tags,player_card_patch,npc_card_patches,dynamic_world_global_background}，补丁合并进 cards。
- **multi_character_stage** 对齐 story_orchestrator：NPC优先/@角色名/每轮一小步/不写最终台词；保留 responseMode=scenario(原 design.md 效果)。
- **speaker** 对齐 story_speaker：不写@前缀/40-80字/动作放(...)不换说话人/不泄漏提示词；修正记忆读取路径。
- **event_manager 彻底对齐**：移除「世界书关键词当章节自动推进」逻辑（踩设计图约束#1红线），不再注入消息/toast、不再改世界书 enabled；退化只读状态面板（发言模式/绑定世界书数/在场角色数）。manifest 去掉 autoAdvance/showPanel，面板 html 改显示只读状态。之前运行时禁用的 event_manager 现已安全，可重新启用。
- 四 entry.js 均 `node --check` 通过；四个 .tpg 已用各自 build.ps1 重建。源码改动见 plugins/ 下四目录。
- **通过 MCP 直接安装插件（已验证可行）**：用户问「mcp 不能安装吗」，确认 MCP 有完整插件管理链（`tavo_plugin_install` 吃 `zipBase64`/`zipPath`、`tavo_plugin_uninstall`、`tavo_plugin_set_enabled`、`tavo_plugin_validate_manifest`、`tavo_plugin_package`）。实测经 `mcp_install_plugins.py` 把四插件目录本地 zipfile 打包→base64→`tavo_plugin_install`→`set_enabled(true)`→`tavo_plugin_get` 验证，四个全部 `ok/enabled=True/v1.0.0` 装好。注意：`tavo_plugin_search` 空 query 才返回全量、按关键词查返回空（搜索匹配字段有限）；本地 build.ps1 在沙箱下 .tpg 未落盘，故改用 zipfile 现打 zip 传 base64 更稳。复用脚本：`.cache/story/谁让这个山大王修仙的/mcp_install_plugins.py`。

## event_manager UI 改「非浮窗·按钮展开抽屉」（已完成）
- 用户要求：故事状态不能是浮动弹窗，参照 toonflow-game 做法——点按钮才展开面板，内含角色信息/参数/故事参数查看。
- 查 `md/tavo_help/plugin_help/plugin.md` 确认 tavo 插件 UI 只有三类：`sidebar`(右侧边栏原生按钮行，非面板)、`htmlFragments`+`/chat`(聊天页常驻块/浮窗，float 须可拖动)、`htmlFragments`+`/messages`。**无原生 drawer**。参照真源 `Toonflow-game-web/src/composables/orchestrationVoiceFlow/story_info.ts` 的「故事信息」范式（按钮展开、汇总世界/章节/回合状态/角色参数）。
- 实现：manifest 移除浮动 `tf-story-panel`(htmlFragments)；改 `sidebar` 两按钮(`tf-story-open` 故事信息 / `tf-story-refresh` 刷新)+ `htmlFragments` 注册 `tf-story-drawer`(src ui/story_panel.html, mount /chat/body/end，初始 `display:none`/translateX(100%))。entry.js 仅保留 refresh handler + open 占位。ui/story_panel.html 重写为**右侧抽屉**(mask+drawer，fixed 右侧全高，点按钮 `sidebar:tf-story-open` 滑出，点遮罩/×收起)，render 时 `await tavo.chat.current()` 拉 角色(头像/名/描述/动态参数卡)、世界书列表、responseMode、记忆(tmm.summary)。版本升 1.0.1，已 MCP 重装启用。
- ⚠️ 待用户界面验证：点右侧边栏「故事信息」按钮抽屉是否滑出、数据是否正确（htmlFragment 内 `tavo.plugin.on('sidebar:...')` 与 `tavo.chat.current()` 可用性依赖 tavo 运行时，未在本地核验）。
- **用户反馈「看不到故事信息按钮」→ 修正 v1.0.2**：根因有二：(1) 侧边栏 `sidebar` 贡献在用户 tavo 里未渲染出按钮；(2) 更致命——`htmlFragment` 内 `tavo.plugin.on('sidebar:tf-story-open')` 收不到侧边栏事件（该事件只派发给 entry.js handler，HTML 片段收不到），所以即便按钮显示也点不开抽屉。改法：**弃用 sidebar，触发按钮直接画进 htmlFragment**（mount `/chat/body/start`，非浮动、始终可见的内联按钮），点击就地 `openDrawer()`，逻辑全在片段内闭环，不再依赖跨上下文事件。manifest 移除 `sidebar`，entry.js 留空注释。已 MCP 重装 1.0.2 启用。教训：tavo 插件里「点按钮展开面板」应让片段自带按钮，不要靠 sidebar+htmlFragment 跨上下文通信。
- 若重装后片段未刷新，让用户**重开该聊天**（或重启 tavo）以重新挂载 htmlFragment。
- **用户反馈「按钮有了但丑，参照 toonflow 形态」→ 修正 v1.0.3**：触发器从顶部丑陋圆角按钮 改为 **聊天页底部一行紧凑 tab**（`{故事名} ›`，橙红 chevron，像 tavo 原生 tab，mount `/chat/body/end`）；面板从右侧抽屉改为**全屏居中模态**，分三段：故事简介 + 角色列表(横滚圆形头像芯片) + 角色详情(两栏键值卡，从 `data.description` 解析 `**字段**：值` 自动生成)。MCP 重装 v1.0.3 启用。
- **用户反馈「打开面板头像不显示」→ 修正（仍 v1.0.3，已重装启用）**：根因 = (1) `tavo.chat.current()` 返回的 `characters` 只带 id/name，不含 `data.avatar`，旧代码 `d.avatar` 为空 → 走「首字占位」兜底；(2) 即便拿到 `files/global/...` 虚拟路径，chat 上下文 webview 也不一定能解析。查 `TavoJS API.md` 确认最稳方案：`tavo.character.get(id)` 拉完整 `data.avatar`，再 `tavo.file.load(path,{encoding:'dataUrl'})` 读回 dataUrl 直接塞 `<img src>`（任何 webview 都渲染），兜底 `tavo.file.url(path)`。面板脚本改为先 `Promise.all` 解析全部角色头像 dataUrl 再渲染芯片/详情。
- 头像渲染铁律（HTML 片段内）：**不要直接 `<img src="files/global/...">`**；要 `tavo.file.load(path,{encoding:'dataUrl'})` 转 dataUrl。虚拟路径在 chat 作用域 webview 解析不可靠。

## 用户身份（persona）问题（晚间）
- `mcp_用户_身份.md` 原本是占位问句（群聊里用户身份=纯小白，怎么以该身份发言）。已解决并补全文档。
- 用户身份 = tavo **persona 资产**（`kind:persona`，字段 name/description/avatar/active），由 `tavo_persona_*` 管理。
- 实测流程：`tavo_persona_create({persona:{name,description,avatar,active:true}})`(返回 id=2) → `tavo_persona_set_active({id})` → `tavo_chat_update({id:6,chat:{personaId:2}})` 绑定群聊 6 → `tavo_message_append({chatId:6,message:{role:'user',content}})` 发言；服务端自动按激活 persona 填 `speakerName`/`speakerAvatar`，身份生效（消息 id=16, speakerName="纯小白"）。
- ⚠️ 设计张力：纯小白同时是群聊 6 的 34 号 NPC 角色卡。用户以纯小白身份发言后聊天里出现「用户纯小白 + NPC纯小白」两个同名实体。如需纯小白纯作玩家，去掉群聊 characterIds 里的 34（待用户决定）。
- 身份描述写法：关系+状态+基调 1-2 句（对齐 docs.tavoai.dev persona 编写心法）。

## 气泡样式插件（toonflow_story_style）
- 按 md/currdesign/气泡样式.md，新建插件（目录 plugins/toonflow_story_style，pluginId com.toonflow.story-style）：**用 TavoJS 而非 MCP 直配**实现气泡样式。文件：manifest.json / entry.js / ui/story_style_panel.html。
- 实现：底部 tab「气泡样式 ›」触发全屏模态；面板含 单条/多条模式 + 透明度滑块(默认35%)。
  - 透明度：走主题 `userBubble.color`/`characterBubble.color` 的 8位色值 AA 通道（35%→0x59），`tavo.theme.update` 递归 patch 只改 color，其他字段「跟随主题」。无主题时 `tavo.theme.create` 并 `tavo.chat.update({theme})` 绑定。
  - 单条模式：`tavo.message.update({hidden:true})` 隐藏除最后一条外所有消息；多条模式全部 `hidden:false` 恢复。监听 `chat:updated` 实时应用单条（新消息来自动藏上一条）。
- entry.js 校验通过；`.tpg` 已生成（5398B，路径 plugins/toonflow_story_style/toonflow_story_style.tpg）。
- **安装踩坑**：首次 `tavo_plugin_install` 报 `invalidManifest / localizationMissing` —— tavo 插件 manifest **必须带 `localization` 字段**（name/description 用 `{"$t":"plugin.name"}` 引用，并配 `localization.defaultLocale` + `resources` 指向 `locales/zh-CN.json`/`locales/en.json`）。仿 event_manager 补上 `locales/zh-CN.json`、`locales/en.json` 后重装成功。
- **已安装成功（19:59）**：`install_style.py` 一键装（`tavo_plugin_install`→`set_enabled(true)`→`tavo_plugin_get` 验证），回查 name="气泡样式"、version=1.0.0、enabled=true。
- trade-off：隐藏的消息不进 AI 上下文（面板提示文案已说明）。
- 提示：若聊天页未出现「气泡样式 ›」底部 tab，重开该聊天（或重启 tavo）重新挂载 htmlFragment。

## 三插件 UI 看不见：同插槽冲突 + memory 浮窗 bug（20:1x）
- 用户反馈：不仅气泡样式看不见，连故事信息(事件管理器)也看不见。MCP 实查：三插件均 installed+enabled=True；但 `htmlFragments` 全挂同一 slot `/chat/body/end`。
- **文档硬前提（plugin.md 顶部 Warning）**：插件 htmlFragment 需在聊天开启「高级渲染」才生效——这是"三个都看不见"最可能根因（之前能看到故事信息 tab，是因那次聊天开了高级渲染；现在这个聊天没开）。
- **第二个真实 bug**：memory_manager 的 ui/panel.html 用了被禁的 `window.tavo.get/plugin.emit`（文档要求直接用 `tavo`），且仍是 `position:fixed` 可拖动浮窗形态（初始无定位、易乱飘/不渲染）。
- **修复**：
  - 重写 memory panel → 「底部 tab + 全屏模态」（与 event/style 同形态），改用 `tavo.get('tmm')` + 3s 轮询读记忆（摘要/事实/标签/玩家&NPC 参数卡）。
  - 分散三 mount：event_manager `body/end`、story_style `body/start`、memory `head/end`，消除同 slot 覆盖。
  - 重装 memory + style 成功（ok/enabled=True，mount 已更新）。
- **待用户侧**：聊天设置开「高级渲染」→ 重开聊天验证三 tab（head/end「记忆 ›」、body/start「气泡样式 ›」、body/end「{故事名} ›」）。若仍无则查片段报错。

## 三插件触发器固定到屏幕视口（20:2x，已完成）
- 用户反馈：重启对话才出；且即便放顶部也不对——触发器应钉在**屏幕**上下部，而非消息滚动区上下部。
- 根因：原触发器是普通文档流元素，挂在消息滚动容器内，随聊天内容一起滚走。
- **修复**：三个 ui 片段的触发器与弹层都改 `position:fixed` 且脚本里 `appendChild` 到 `document.body`（防被聊天容器的 transform 祖先影响导致 fixed 失效）。位置：
  - 气泡样式 → 屏幕**左上** (`top:8px;left:8px`)
  - 记忆 → 屏幕**右上** (`top:8px;right:8px`)
  - 故事信息(事件管理器) → 屏幕**底部居中** (`bottom:10px;left:50%;translateX(-50%)`)，对齐 toonflow 底部 bar 形态
  - 弹层 `z-index:10000` 高于触发器 `9990`，开启时遮住触发器。
  - 脚本加 `pinToBody(id)`：重挂载时先移除旧实例再挂新，避免重复。
- 重装三插件成功（zip 真实大小 13.8KB/7.6KB/11.3KB，ok/enabled=True）。复用脚本 `.cache/story/谁让这个山大王修仙的/install_all_fixed.py`（注意：目录映射必须显式写全，用 `com.toonflow.x`.replace 会把连字符保留导致路径错、打成 32B 空包）。
- **仍须知**：htmlFragment 只在聊天打开/重开时挂载一次，「改完需重开该聊天」这点无法绕过（tavo 行为）。
- 顺带确认 `md/currdesign/气泡样式.md` 里插件名拼写正确（toonflow_story_style，无 stytle 错字）。
